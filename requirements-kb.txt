# api_kb.py doesn't do local embedding or PDF text extraction — the
# Knowledge Base handles chunking/embedding/PDF-parsing during S3 sync.
# So this skips sentence-transformers, torch, numpy, and pypdf entirely,
# which keeps the Docker image much smaller and the build much faster
# than requirements.txt (the local-pipeline version).
google-genai>=0.3.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
boto3>=1.34.0
trafilatura>=1.12.0
pypdf>=4.0.0